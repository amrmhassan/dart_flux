name: dart_flux
description: >
  A lightweight and scalable Dart backend framework with built-in 
  web server, authentication, file management, database etc...
version: 0.0.2
homepage: https://github.com/amrmhassan/dart_flux.git

environment:
  sdk: ^3.7.2

dependencies:
  # path: ^1.8.0


dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0